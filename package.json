{
  "name": "horizon-base-theme",
  "version": "1.0.0",
  "description": "Shopify Horizon theme with native per-component CSS ({% stylesheet %})",
  "scripts": {
    "dev:shopify": "npm run theme:dev",
    "theme:dev": "dotenv -e .env -- sh -c 'shopify theme dev --store=$SHOPIFY_STORE'"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.1"
  }
}
